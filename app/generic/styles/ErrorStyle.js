import { StyleSheet } from "react-native";

const errorStyle = StyleSheet.create({
    errorText: {
        color: 'red',
        fontSize: 16,
        margin: 16
    },
});

export default errorStyle;
